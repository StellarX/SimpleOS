项目说明：   一个32位的操作系统

z_tools文件夹：项目工具

os文件夹内：（主要项目文件）
ipl.nas：启动程序加载器（将OS从磁盘读入内存）
Makefile：包括文件生成规则和命令
make.bat：调用make.exe(这个内部的命令不懂)，而make.exe的作用是进入makefile文件，然后执行里面的命令
run.bat：运行qemu pc模拟器

本次改动说明：
在ipl.nas中添加读盘错误判断、将磁盘的前10个柱面读到内存的代码
（读盘程序的底层还是调用了BIOS的0x13号函数）

现在已经能够把软盘最初的10 × 2 × 18 × 512 = 184 320 byte=180KB内容装载到内存
（从C0-H0-S18 到 C9-H1-S18）